#sign_in1
  .container
    .row
      .span12.header
        %h4 Sign in to your account
        %p
          There are many variations of passages of Lorem alteration in some form  injected humour these randomised words .
        .span4.social
          %a.circle.facebook{:href => user_omniauth_authorize_path(:facebook)}
            %img{:alt => "", :src => asset_path("face.png")}/
          %a.circle.twitter{:href => user_omniauth_authorize_path(:twitter)}
            %img{:alt => "", :src => asset_path("twt.png")}/
          %a.circle.gplus{:href => user_omniauth_authorize_path(:gplus)}
            %img{:alt => "", :src => asset_path("gplus.png")}/
      .span3.division
        .line.l
        %span or
        .line.r
      = form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f|
        .span12.footer
          = f.text_field :email, :autofocus => true, :placeholder => "Email"
          = f.password_field :password, :placeholder => "Password"
          %input{:placeholder => "Confirm Password", :type => "submit", :value => "sign in"}/
        .span12.proof
          .span5.remember
            %label.checkbox
              = f.check_box :remember_me
              = f.label :remember_me
            = link_to "Forgot your password?", new_password_path(resource_name)
          .span3.dosnt
            %span Don’t have an account?
            %a{:href => new_registration_path(resource_name)} Sign up